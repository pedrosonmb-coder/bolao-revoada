// Tailwind CSS v4 usa configuração CSS-first via @theme em globals.css.
// Este arquivo existe para compatibilidade e darkMode: 'class'.
// Cores e fontes estão definidas em src/app/globals.css via @theme.
const config = {
  darkMode: 'class',
}

export default config
