export function Header() {
  return (
    <header className="sticky top-0 z-40 bg-(--color-bg-base) border-b border-(--color-border-base)">
      <div className="flex items-center justify-center h-12 px-4">
        <span className="font-[family-name:var(--font-tight)] font-black text-base uppercase tracking-wide text-(--color-text-primary)">
          Bolão do Revoada
        </span>
      </div>
    </header>
  )
}
